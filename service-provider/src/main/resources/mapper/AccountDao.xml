<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.AccountDao">

    <insert id="addAccount">
        INSERT INTO account (wechat_id, owner_id, owner_kind)
        VALUE (#{wechatId}, #{ownerId}, #{ownerKind})
    </insert>

    <select id="getAccountId" resultType="int">
        SELECT account_id FROM account
        WHERE wechat_id = #{wechatId} AND owner_id = #{ownerId}
        AND owner_kind = #{ownerKind}
    </select>

    <select id="getBalance">
        SELECT balance FROM account WHERE account_id = #{accountId}
    </select>

    <update id="updateBalance">
        UPDATE account SET balance = balance + #{value}
        WHERE account_id = #{accountId} AND balance + #{value} >= 0
    </update>

    <delete id="deleteAccount">
        DELETE FROM account WHERE account_id = #{accountId}
    </delete>

</mapper>